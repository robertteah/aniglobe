services:
  - type: static
    name: zenime
    env: static
    buildCommand: npm install && npm run build
    publishPath: ./dist
    routes:
      - type: rewrite
        source: /[^.]+
        destination: /index.html
